Things to do in the YAML
1) Make a service account 
2) Create a DB instance 
3) Create a user, grant privileges, and create a database
4) Make a VM instance - add a startup script.
One for the database, one for the pub sub, one for the web server
5) Make a firewall rule
6) Make a pub sub topic
